<h1>These are Bad Cats</h1>

<ul>
  <% @cats.each do |cat| %>
    <li><a href="<%= cat_url(cat) %>"><%= cat.name %></a>
    <li><%= cat.description %></li>
    </li>

  <% end %>
</ul>

<a href="<%= new_cat_url %>">Report cat</a>
